{
    "tasks": [
        {
            "label": "make format",
            "type": "shell",
            "command": "/usr/bin/make",
            "args": [
                "format",
                "-j",
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "label": "make test_main",
            "type": "shell",
            "command": "/usr/bin/make",
            "args": [
                "test_main",
                "-j",
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
    ],
    "version": "2.0.0"
}
